spring:
  application:
    name: fotolhar-api

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

  datasource:
    url: jdbc:postgresql://localhost:5432/fotolhar_db
    username: fotolhar_user
    password: fotolhar_senha123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc.time_zone: America/Sao_Paulo

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail.smtp.auth: ${MAIL_SMTP_AUTH:true}
      mail.smtp.starttls.enable: ${MAIL_SMTP_STARTTLS:true}

management:
  health:
    mail:
      enabled: false
  endpoints:
    web:
      exposure:
        include: health, info

jwt:
  secret: 8f42a3c1e9d7b6f05a2c4e8d1f3b7a9c2e5d8f1a4b7c0e3f6a9d2b5e8c1f4a7
  expiration: 86400000

cloudinary:
  cloud-name: ddwei8qbu
  api-key: 776843477352766
  api-secret: XGkFTftRE5VCS7aT9ho32P_W6fc

app:
  frontend-url: ${APP_FRONTEND_URL:http://localhost:5173}

server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    com.fotolhar: DEBUG
    org.hibernate.SQL: INFO
    org.springframework: WARN
